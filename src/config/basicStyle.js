const rowStyle = {
  width: '100%',
  display: 'flex',
  flexFlow: 'row wrap',
};
const colStyle = {
  marginBottom: '16px',
};

const collapseStyle = {
  marginBottom: '16px',
};

const stickerColStyle = {
  marginBottom: '0px',
};

const gutter = 16;
const basicStyle = {
  rowStyle,
  colStyle,
  collapseStyle,
  stickerColStyle,
  gutter,
};

export default basicStyle;
