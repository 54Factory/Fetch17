import React from 'react';
import { Button } from 'antd'

export default class UpdateButton extends React.Component {
  render() {

    return (
      <Button className="isoUpdateCustomerBtn" type="primary">Edit</Button>
    );
  }
}
